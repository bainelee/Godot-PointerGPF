# Godot 4.6 — 调试游戏窗口收尾手测矩阵（计划 Task 5）

在真实机器上填写；用于验证「`(DEBUG)` 窗口关闭、编辑器仍开」与 `project_close.debug_game_teardown_ok` 等字段。

| 步骤 | 操作 | 预期 |
|------|------|------|
| 1 | 打开 `examples/godot_minimal`，确认已启用 `addons/pointer_gpf` | 插件加载无报错 |
| 2 | F5 运行主场景，确认出现标题含 `(DEBUG)` 的窗口 | 与截图 2 同类 |
| 3 | 触发 MCP `run_game_basic_test_flow*`（或仅桥接 `closeProject`） | `pointer_gpf/tmp/auto_stop_play_mode.flag` 被消费 |
| 4 | 目检 2 秒内 `(DEBUG)` 窗口关闭 | 编辑器主窗口仍开 |
| 5 | 若窗口未关，读取 `pointer_gpf/tmp/teardown_debug_game_last.json` | 若存在且 `ok:false`，将 `reason` 记入本表 |
| 6 | 读取工具返回 `project_close.debug_game_teardown_ok` | `false` 时与 JSON 原因一致 |

**Editor 设置记录（若与嵌入/独立窗口有关）：**（运行菜单 / Editor Settings 中与 Run 窗口相关项）

**Godot 版本：** _______________

**填写人 / 日期：** _______________
