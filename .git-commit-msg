feat: externalize PointerGPF config and release flow

Ship portable MCP tooling with plugin lifecycle commands, project context initialization, flow seed generation, adapter contract docs, and scripts/workflows for cross-project verification and release manifest backfill.